{
  "scripts": {
    "build": "npx tsc",
    "buildw": "npx tsc -w",
    "dev-http": "cd http-dev-server && node serve.cjs"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
